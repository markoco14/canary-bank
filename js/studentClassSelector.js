//this script only updates the Make New Student section
//it updates at onload
//so every time a teacher's class list is edited
//the page refreshes and the selector is reset

//create a reference to the selector
//write the script to populate the options



/*
console.log(classNames.length);
for (i = 0; i < classNames.length; i++) {
	console.log(i);
}*/